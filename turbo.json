{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "//#_format": {
      "inputs": [
        "*.{cjs,js,jsx,ts,tsx,yaml,yml,json,md,mdx,css,html,graphql}",
        "{.github,apis,circuits,demo,infra,membership,ui}/**/*.{cjs,js,jsx,ts,tsx,yaml,yml,json,md,mdx,css,html,graphql}",
        "!{apis,demo,infra,membership,ui}/**/{build,dist,node_modules,.next}/**"
      ]
    },
    "_format": {
      "cache": false,
      "inputs": ["contracts/{script,src,test}/**/*.sol"]
    },
    "_format.fix": {
      "cache": false,
      "inputs": ["contracts/{script,src,test}/**/*.sol"]
    },
    "//#_format.fix": {
      "inputs": [
        "*.{cjs,js,jsx,ts,tsx,yaml,yml,json,md,mdx,css,html,graphql}",
        "{.github,apis,circuits,demo,infra,membership,ui}/**/*.{cjs,js,jsx,ts,tsx,yaml,yml,json,md,mdx,css,html,graphql}",
        "!{apis,circuits,demo,infra,membership,ui}/**/{build,dist,node_modules,.next}/**"
      ]
    },
    "_lint": {
      "cache": false,
      "inputs": ["contracts/{script,src,test}/**/*.sol"]
    },
    "//#_lint": {
      "inputs": [
        "*.c?{j,t}s?(x)",
        "{apis,circuits,demo,infra,membership,ui}/**/*.c?{j,t}s?(x)",
        "!{apis,circuits,demo,infra,membership,ui}/**/{build,dist,node_modules,.next}/**"
      ]
    },
    "_lint.fix": {
      "cache": false,
      "inputs": ["contracts/{script,src,test}/**/*.sol"]
    },
    "//#_lint.fix": {
      "inputs": [
        "*.c?{j,t}s?(x)",
        "{apis,circuits,demo,infra,membership,ui}/**/*.c?{j,t}s?(x)",
        "!{apis,circuits,demo,infra,membership,ui}/**/{build,dist,node_modules,.next}/**"
      ]
    },
    "typecheck": {
      "inputs": [
        "*.{j,t}s?(x)",
        "{apis,circuits,demo,infra,membership,ui}/**/{src,test}/**/*.{j,t}s?(x)",
        "!{apis,circuits,demo,infra,membership,ui}/**/{build,dist,node_modules,.next}/**"
      ]
    },
    "build": {
      "dependsOn": ["^build", "build.graph"],
      "outputs": [
        "{apis,demo,membership,ui}/**/{build,dist,.next}/**",
        "!{apis,demo,membership,ui}/**/node_modules/**"
      ]
    },
    "build.graph": {
      "outputs": ["src/lib/graph/.graphclient/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": [
        "{apis/{prove,query},membership,circuits/circom}/{src,test}/**/*.{j,t}s?(x)",
        "contracts/src/**/*.sol"
      ]
    },
    "deploy": {
      "dependsOn": ["test"]
    }
  }
}
