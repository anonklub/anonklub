{
  "name": "@anonklub/halo2-wasm-ext",
  "private": true,
  "scripts": {
    "format.cargo": "cargo fmt -- --check",
    "format.cargo.fix": "cargo fmt",
    "lint.cargo": "cargo clippy",
    "lint.cargo.fix": "test -d node_modules || exit 0 && cargo clippy --fix"
  }
}
